---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: frontend-partials
  namespace: public
  labels:
    k8s-app: frontend-partials
spec:
  replicas: 2
  selector:
    matchLabels:
      k8s-app: frontend-partials
  template:
    metadata:
      labels:
        k8s-app: frontend-partials
    spec:
      containers:
      - name: frontend-partials
        image: registry.gitlab.com/apolitical/frontend-partials:v1.1.0-beta.1.0
        imagePullPolicy: "IfNotPresent"
        ports:
        - containerPort: 80
        env:
        - name: GOOGLE_TAG_MANAGER_ID
          valueFrom:
            secretKeyRef:
              name: google-tag-manager-id
              key: GOOGLE_TAG_MANAGER_ID
      imagePullSecrets:
      - name: gitlab-registry-frontend-partials

---
apiVersion: v1
kind: Service
metadata:
  name: frontend-partials
  namespace: public
  labels:
    k8s-app: frontend-partials
spec:
  selector:
    k8s-app: frontend-partials
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  type: NodePort
